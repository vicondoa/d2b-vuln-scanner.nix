# GitHub operations

The repository uses branch protection, Makefile-driven CI, a changelog gate,
least-privilege workflow permissions, pinned actions or documented pinning
policy, and scheduled flake-lock update PRs.

