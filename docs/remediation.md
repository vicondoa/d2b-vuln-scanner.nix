# Remediation

Remediation is disabled by default.

`d2b-vuln-remediate` writes a prompt file and launches a configured argv vector.
Bundled GitHub and Claude skills are intended for vulnerability-fixing agents
that work on scanner findings in consumer repositories. They are not the
contributor workflow for this repository.

